# InfluxDB Configuration for Perfornium
# Copy this file to .env and configure as needed
#
# InfluxDB is OPTIONAL. Without these settings:
# - Infrastructure metrics: stored in-memory (real-time only)
# - Test metrics: stored in JSON files (default behavior)
#
# With InfluxDB enabled:
# - Infrastructure metrics: persistent storage with time-range queries
# - Test metrics: can also be stored in InfluxDB for advanced querying

# InfluxDB connection URL
INFLUXDB_URL=http://localhost:8086

# InfluxDB authentication token
# If using docker-compose.yml default setup, use: perfornium-token
INFLUXDB_TOKEN=perfornium-token

# InfluxDB organization name
INFLUXDB_ORG=perfornium

# InfluxDB bucket name for metrics storage
INFLUXDB_BUCKET=metrics
